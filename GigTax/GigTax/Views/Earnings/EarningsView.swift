import SwiftUI

struct EarningsView: View {
    var body: some View {
        NavigationStack {
            Text("Earnings import coming in S3")
                .foregroundStyle(.secondary)
                .navigationTitle("Earnings")
        }
    }
}

#Preview {
    EarningsView()
}
